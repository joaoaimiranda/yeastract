const dbaccess = require("../db/dbaccess");
var express = require("express");
var router = express.Router();

router.get("/:term", advancedSearch);

function advancedSearch(idk) {
    // XDDDDDDDDDDDDDDDDDDDDDD
}
